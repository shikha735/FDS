<?php
// ---------------------------------------------------------
 $app_name = "phpJobScheduler";
 $phpJobScheduler_version = "3.9";
// ---------------------------------------------------------

define('DBHOST', 'localhost');// database host address - localhost is usually fine

define('DBNAME', 'fraud');// database name - must already exist
define('DBUSER', 'root');// database username - must already exist
define('DBPASS', '');// database password for above username

define('DEBUG', false);// set to false when done testing